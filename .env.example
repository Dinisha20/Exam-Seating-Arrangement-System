# Copy to .env and adjust if running outside Docker Compose.
# Docker Compose sets DATABASE_URL automatically (see docker-compose.yml) —
# you only need this file for local (non-Docker) development.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/exam_seating
